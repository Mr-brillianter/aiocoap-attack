---
type: "always_apply"
description: "Example description"
---

- Always place new test files in the existing tests/ directory. Never create tests unless the task explicitly says "add tests" or "write unit tests".
- Do not generate or update README.md, docs/, or any documentation files unless specifically requested.
- Never create files in the root directory. Follow the existing project structure strictly.
- Prefer minimal changes: only generate necessary code, avoid over-engineering.